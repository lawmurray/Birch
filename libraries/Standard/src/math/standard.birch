/**
 * Error function.
 */
function erf(x:Real) -> Real {
  cpp {{
  return ::erf(x);
  }}
}

/**
 * Complementary error function.
 */
function erfc(x:Real) -> Real {
  cpp {{
  return ::erfc(x);
  }}
}
