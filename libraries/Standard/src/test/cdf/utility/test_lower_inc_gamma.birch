/*
 * Testing `lower_inc_gamma`
 */

program test_lower_inc_gamma(a:Real64, x:Real64) {
    // note that this is not a robust test
    // TODO(pranavs): Have a streamlined test for this function
    a <- 2.0;
    x <- 3.0;
    let temp <- lower_inc_gamma(a, x);
    stdout.print(temp);
}
