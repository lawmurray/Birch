/*
 * Test scaled-gamma-exponential grad evaluations.
 */
program test_grad_scaled_gamma_exponential(N:Integer <- 1000, backward:Boolean <- false) {
  m:TestScaledGammaExponential;
  test_grad(m, N, backward);
}
