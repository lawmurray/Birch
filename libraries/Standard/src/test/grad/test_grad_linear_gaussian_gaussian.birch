/*
 * Test linear Gaussian-Gaussian grad evaluations.
 */
program test_grad_linear_gaussian_gaussian(N:Integer <- 1000, backward:Boolean <- false) {
  m:TestLinearGaussianGaussian;
  test_grad(m, N, backward);
}
