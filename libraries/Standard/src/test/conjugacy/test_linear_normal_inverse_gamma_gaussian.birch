/*
 * Test linear inverse-gamma-Gaussian conjugacy.
 */
program test_linear_normal_inverse_gamma_gaussian(N:Integer <- 10000, lazy:Boolean <- false) {
  m:TestLinearNormalInverseGammaGaussian;
  test_conjugacy(m, N, 3, lazy);
}
