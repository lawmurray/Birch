/*
 * Test enumeration of discrete random variables.
 */
program test_subtract_discrete_delta(N:Integer <- 10000,
    lazy:Boolean <- false) {
  m:TestSubtractDiscreteDelta;
  test_conjugacy(m, N, lazy);
}
