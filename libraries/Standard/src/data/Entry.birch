/**
 * Entry in an object.
 */
final class Entry(key:String, value:Buffer) {
  /**
   * Key.
   */
  key:String <- key;
  
  /**
   * Value.
   */
  value:Buffer <- value;
}

/**
 * Create an Entry.
 */
function Entry(key:String, value:Buffer) -> Entry {
  return construct<Entry>(key, value);
}
