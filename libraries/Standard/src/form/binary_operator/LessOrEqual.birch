struct LessOrEqual<Left,Right>(l:Left, r:Right) < Binary<Left,Right>(l, r) {
  //
}

function value<Left,Right>(x:LessOrEqual<Left,Right>) -> {
  return value(x.l) <= value(x.r);
}

function peek<Left,Right>(x:LessOrEqual<Left,Right>) -> {
  return peek(x.l) <= peek(x.r);
}

function eval<Left,Right>(x:LessOrEqual<Left,Right>) -> {
  return eval(x.l) <= eval(x.r);
}

function grad<Left,Right,Gradient>(x:LessOrEqual<Left,Right>, d:Gradient) {
  grad(x.l, 0.0);
  grad(x.r, 0.0);
}

hpp{{
namespace birch {
/**
 * Less than or equal to comparison.
 */
template<class Left, class Right, std::enable_if_t<is_delay<Left>::value ||
    is_delay<Right>::value,int> = 0>
LessOrEqual<Left,Right> operator<=(Left&& l, Right&& r) {
  return construct<LessOrEqual<Left,Right>>(std::forward<Left>(l),
      std::forward<Right>(r));
}
}
}}
