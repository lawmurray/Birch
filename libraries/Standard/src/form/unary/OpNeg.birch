struct OpNeg<Middle>(m:Middle) < Unary<Middle>(m) {
  /**
   * Memoized result.
   */
  phantom x;

  hpp{{
  BIRCH_UNARY_FUNCTION_FORM(operator-, op_neg_grad)
  }}
}

hpp{{
namespace birch {
using numbirch::operator-;
using numbirch::op_neg_grad;

/**
 * Negation.
 */
template<class Middle, std::enable_if_t<is_delay<Middle>::value,int> = 0>
OpNeg<Middle> operator-(const Middle& m) {
  return construct<OpNeg<Middle>>(m);
}
}
}}
