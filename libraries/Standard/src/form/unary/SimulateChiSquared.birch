struct SimulateChiSquared<Middle>(m:Middle) < Unary<Middle>(m) {
  /**
   * Memoized result.
   */
  phantom x;

  hpp{{
  BIRCH_UNARY_FUNCTION_FORM(simulate_chi_squared)
  BIRCH_FUNCTION_FORM(simulate_chi_squared)
  }}
}

hpp{{
namespace birch {
using numbirch::simulate_chi_squared;

template<class Middle, std::enable_if_t<is_delay_v<Middle>,int> = 0>
SimulateChiSquared<Middle> simulate_chi_squared(const Middle& m) {
  return construct<SimulateChiSquared<Middle>>(m);
}
}
}}
