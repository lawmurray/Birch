struct SimulateWeibull<Left,Right>(l:Left, r:Right) < Binary<Left,Right>(l, r) {
  /**
   * Memoized result.
   */
  phantom x;

  hpp{{
  BIRCH_BINARY_FUNCTION_FORM(simulate_weibull)
  BIRCH_FUNCTION_FORM(simulate_weibull)
  }}
}

hpp{{
namespace birch {
using numbirch::simulate_weibull;

template<class Left, class Right, std::enable_if_t<
    is_delay_v<Left,Right>,int> = 0>
SimulateWeibull<Left,Right> simulate_weibull(const Left& l, const Right& r) {
  return construct<SimulateWeibull<Left,Right>>(l, r);
}

}
}}
