struct SimulateGaussian<Left,Right>(l:Left, r:Right) < Binary<Left,Right>(l, r) {
  /**
   * Memoized result.
   */
  phantom x;

  hpp{{
  BIRCH_BINARY_FUNCTION_FORM(simulate_gaussian)
  BIRCH_FUNCTION_FORM(simulate_gaussian)
  }}
}

hpp{{
namespace birch {
using numbirch::simulate_gaussian;

template<class Left, class Right, std::enable_if_t<
    is_delay_v<Left,Right>,int> = 0>
SimulateGaussian<Left,Right> simulate_gaussian(const Left& l, const Right& r) {
  return construct<SimulateGaussian<Left,Right>>(l, r);
}

}
}}
