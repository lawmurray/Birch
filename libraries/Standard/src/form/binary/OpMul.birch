struct OpMul<Left,Right>(l:Left, r:Right) < Binary<Left,Right>(l, r) {
  /**
   * Memoized result.
   */
  phantom x;

  hpp{{
  BIRCH_BINARY_FUNCTION_FORM(operator*, op_mul_grad)
  }}
}

hpp{{
namespace birch {
using numbirch::operator*;
using numbirch::op_mul_grad;

/**
 * Multiplication.
 */
template<class Left, class Right, std::enable_if_t<
    is_delay<Left,Right>::value,int> = 0>
OpMul<Left,Right> operator*(const Left& l, const Right& r) {
  return construct<OpMul<Left,Right>>(l, r);
}
}
}}
