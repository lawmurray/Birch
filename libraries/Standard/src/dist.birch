/**
 * Build a source tarball for the package, for distribution.
 *
 *     birch dist
 *
 * This creates a source tarball `Name-version.tar.gz` in the package
 * directory containing all of the Birch sources.
 *
 * If you would prefer to create a source tarball of the C++ sources only, use
 * [configure](../configure) followed by `make dist` instead.
 */
program dist();
