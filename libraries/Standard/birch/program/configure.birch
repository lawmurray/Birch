/**
 * Configure the package.
 *
 *     birch configure
 *
 * Accepts the same options as [build](../build).
 *
 * @note
 *     Usually one just calls [build](../build), which calls `configure`
 *     implicitly.
 */
program configure();
