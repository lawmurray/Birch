/**
 * Abstract parser.
 */
class Parser {  
  /**
   * Parse a file.
   *
   * - path: File path.
   * - buffer: Buffer into which to load contents.
   */
  function parse(path:String, buffer:Buffer);
}
