/**
 * Rao--Blackwellized particle filter. This is an alias of ParticleFilter,
 * as locally-optimal proposals and marginalization are (mostly) already
 * handled by that class as a result of its delayed sampling support.
 */
class RaoBlackwellizedParticleFilter = ParticleFilter;
