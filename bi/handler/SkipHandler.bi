/**
 * Event handler that skips events in trace. This is typically followed
 * by another event handler that first applies actions to the events.
 */
final class SkipHandler < TraceHandler {
  function handle(event:Event, record:Record) -> Real {
    //
  }
}
