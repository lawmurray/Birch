/**
 * Event handler that proposes a trace of events.
 */
class ProposalHandler < Handler {
  /**
   * The trace of events.
   */
  trace:List<Event>;

  function handle(evt:FactorEvent) -> Real {

  }
  
  function handle(evt:RandomEvent) -> Real {

  }
}
