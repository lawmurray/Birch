/**
 * Exchange between compartments of an SEIR model at a single time step.
 */
class SEIRExchange {
  s:Integer;  // trials in susceptible population
  e:Integer;  // trials in exposed population
  i:Integer;  // trials in infectious population
}
