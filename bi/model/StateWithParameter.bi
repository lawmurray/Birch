/**
 * Concept for a state with a parameter.
 */
class StateWithParameter {
  /**
   * Simulate initial state.
   *
   * - θ: Parameter.
   */
  fiber simulate(θ:Parameter) -> Real! {
    //
  }

  /**
   * Simulate transition.
   *
   * - x: Previous state.
   * - θ: Parameter.
   */
  fiber simulate(x:StateWithParameter, θ:Parameter) -> Real! {
    //
  }
}
