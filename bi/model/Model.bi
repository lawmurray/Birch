/**
 * Base class for all models, enabling runtime loading of model classes.
 */
class Model {
  /**
   * Simulate.
   */
  fiber simulate() -> Real {
    assert false;
  }
  
  /**
   * Log-weight.
   */
  w:Real <- 0.0;
}
