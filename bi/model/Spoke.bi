/**
 * Spoke concept.
 */
class Spoke {
  /**
   * Simulate.
   *
   * - θ: Parameter.
   */
  fiber simulate(θ:Parameter) -> Real;
  
  /**
   * Input.
   */
  function input(reader:Reader) {
    //
  }
  
  /**
   * Output.
   */
  function output(writer:Writer) {
    //
  }
}
